# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DirectThread {
  createdAt: DateTime!
  id: ID!
  latestSequence: Int!
  messages(afterSequence: Int, beforeSequence: Int, limit: Int): [Message!]!
  updatedAt: DateTime!
  userA: User
  userAId: ID!
  userB: User
  userBId: ID!
}

type Group {
  createdAt: DateTime!
  id: ID!
  members: [GroupMember!]!
  messages(afterSequence: Int, beforeSequence: Int, limit: Int): [Message!]!
  updatedAt: DateTime!
}

type GroupMember {
  groupId: ID!
  joinedAt: DateTime!
  role: String!
  user: User
  userId: ID!
}

"""Arbitrary JSON value"""
scalar JSON

"""Type of media attached to a message"""
enum MediaType {
  AUDIO
  DOCUMENT
  IMAGE
  VIDEO
}

type Message {
  contentType: MessageContentType!
  createdAt: DateTime!
  deletedAt: DateTime
  editedAt: DateTime
  forwarded: Boolean!
  groupId: ID
  id: ID!
  media: [MessageMedia!]!
  replyTo: Message
  replyToMessageId: ID
  senderDeviceId: ID!
  senderUserId: ID!
  threadId: ID
  threadSequence: Int!
}

"""Type of content in a message"""
enum MessageContentType {
  LOCATION
  TEXT
  VENUE
}

type MessageMedia {
  createdAt: DateTime!
  duration: Int
  filename: String!
  height: Int
  id: ID!
  mediaCiphertextHash: String!
  mediaType: MediaType!
  messageId: ID!
  mimeType: String!
  sizeBytes: Int!
  thumbnailBucketKey: String
  thumbnailCiphertextHash: String
  uploadSessionId: String
  uploadStatus: UploadStatus!
  uploadedAt: DateTime
  width: Int
}

type Query {
  directThread(id: ID!): DirectThread
  directThreads(userId: ID!): [DirectThread!]!
  group(id: ID!): Group
  groups(userId: ID!): [Group!]!
}

"""Upload status of a media attachment"""
enum UploadStatus {
  COMPLETE
  FAILED
  PENDING
}

type User {
  createdAt: DateTime!
  encryptedProfile: JSON
  id: ID!
  profileAvatarUrl: String
  profileKeyHash: String
  username: String
}